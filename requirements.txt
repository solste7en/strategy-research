# Data — primary source (Alpaca 1-min SIP bars, free paper account, ~3yr history)
alpaca-py>=0.20.0

# Data processing
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.26.0

# Schwab API (backup source — only needed for SchwabBarsProvider / --source schwab)
schwab-py

# Credentials from .env file (ALPACA_API_KEY, ALPACA_API_SECRET)
python-dotenv>=1.0.0

# Testing & reporting
pytest>=8.0.0
pytest-cov>=6.0.0
tabulate>=0.9.0        # required by pandas .to_markdown()

# Linting
ruff>=0.8.0

# Data — primary source
alpaca-py>=0.20.0          # 1-min SIP bars, 3yr history, free account

# Data
pandas>=2.0.0
pyarrow>=14.0.0

# Schwab API (backup source — only needed for SchwabBarsProvider)
schwab-py

# Credentials from .env file
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-cov>=6.0.0

# Linting
ruff>=0.8.0
